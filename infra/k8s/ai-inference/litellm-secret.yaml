# TEMPLATE: Replace placeholder values before applying.
# Do NOT commit actual secrets to git.
#
# Usage:
#   kubectl create secret generic litellm-secrets #     --namespace ai-inference #     --from-literal=anthropic-api-key=sk-ant-XXXXX #     --from-literal=master-key=sk-eagles-XXXXX
#
# Or apply this template after base64-encoding your values:
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: ai-inference
  labels:
    app: litellm-gateway
    app.kubernetes.io/part-of: eagles-ai-platform
type: Opaque
data:
  # echo -n "your-anthropic-api-key" | base64
  anthropic-api-key: REPLACE_WITH_BASE64_ENCODED_VALUE
  # echo -n "your-litellm-master-key" | base64
  master-key: REPLACE_WITH_BASE64_ENCODED_VALUE
