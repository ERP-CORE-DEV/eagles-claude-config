{
  "name": "eagles-advanced",
  "version": "0.1.0",
  "private": true,
  "description": "EAGLES Advanced — token tracking, vector memory, and drift detection MCPs",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "build:ordered": "pnpm run build:shared && pnpm run build:data && pnpm run build:mcps && pnpm run build:bench",
    "build:shared": "pnpm --filter @eagles-advanced/shared-utils run build",
    "build:data": "pnpm --filter @eagles-advanced/data-layer run build",
    "build:mcps": "pnpm --filter @eagles-advanced/token-tracker-mcp --filter @eagles-advanced/vector-memory-mcp --filter @eagles-advanced/drift-detector-mcp run build",
    "build:bench": "pnpm --filter @eagles-advanced/benchmark run build",
    "test": "vitest run --workspace vitest.workspace.ts",
    "test:coverage": "vitest run --workspace vitest.workspace.ts --coverage",
    "test:watch": "vitest --workspace vitest.workspace.ts",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean",
    "setup": "bash scripts/setup.sh",
    "benchmark": "pnpm --filter @eagles-advanced/benchmark run start",
    "smoke-test": "bash scripts/smoke-test.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
